Fix included:
- Registers the backward-compatible validator service ID digital_card_social_link_url.
- Keeps the modern DigitalCardSocialLinkUrl constraint plugin.
- Makes the validator support both text/url fields and Drupal link fields.
- After extracting, run drush cr.
